# Minimal dependencies to run the Explorer's offline unit tests.
# The full runtime (gremlinpython, opensearch-py, sentence-transformers, …) is
# only needed for the live backend; tests that require it skip when it's absent.
pytest>=8.0
PyYAML>=6.0
SQLAlchemy>=2.0
pytest-cov>=5.0
allure-pytest>=2.13
