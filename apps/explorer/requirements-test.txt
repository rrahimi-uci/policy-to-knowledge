# Dependencies to run the Explorer's offline unit tests.
# The heavy live-backend extras (opensearch-py, sentence-transformers) are NOT
# needed; those modules are excluded from unit coverage and covered by e2e.
pytest>=9.1.1
pytest-cov>=7.1.0
allure-pytest>=2.13
PyYAML>=6.0
SQLAlchemy>=2.0
python-dotenv>=1.0
redis>=5.0
gremlinpython>=3.7
