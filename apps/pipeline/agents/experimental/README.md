# Experimental Agents

Prototype agents that are **not** part of the numbered 1–10 extraction and
comparison pipeline and are not imported by `cli/extract.py` or `cli/compare.py`.
They are kept here for reference and future work.

| Module | Purpose |
| --- | --- |
| `agent_jtbd_kg_mapper.py` | Maps Jobs-To-Be-Done onto a knowledge graph (prototype) |

See [../README.md](../README.md) for the production agent reference.
