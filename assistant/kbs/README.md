# Knowledge Bases (`kbs/`)

This directory holds the source document chunks the Explorer resolves when you
click a rule's reference. Each graph in [`conf/graphs.yaml`](../conf/graphs.yaml)
points here via its `docs_folder` field (e.g. `kbs/sample-guidelines`).

**No data ships with this repository.** Supply your own document folders here.
