# Development & test dependencies for the Pipeline (CLI + UI backend + tests).
# Install with:  pip install -r requirements-dev.txt
-r requirements.txt

# UI backend (FastAPI) — not needed for the CLI pipeline itself
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
httpx>=0.27
websockets>=12.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
